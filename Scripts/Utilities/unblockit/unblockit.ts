/**
 * Companion function to blockIt. Unblocks the target div.
 * @param unblockTarget
 */
function unblockit(unblockTarget) {
  unblockTarget && unblockTarget.unblock();
}

export default unblockit;
